# Copy to `.env` and adjust as needed.
# YugabyteDB YSQL is wire-compatible with Postgres — use any pg env vars.
PGHOST=localhost
PGPORT=5433
PGUSER=yugabyte
PGPASSWORD=
PGDATABASE=orders_app
PORT=3000
